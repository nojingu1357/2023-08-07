doctype html 
html 
    head
        meta(charset="utf-8") 

        body 
            h1 
                a(href="/main") Go to MainPage 
            ol
                each titles in topics
                    li 
                        - rid = encodeURIComponent(titles["@rid"])
                        a(href="/main/"+rid)= titles.title
                br
                - rid = encodeURIComponent(id)
                form(action="/main/"+rid+"/deleted" method="post") 
                    button(type="submit") YES! 
                form(action="/main") 
                    button(type="submit") NO!!                   

